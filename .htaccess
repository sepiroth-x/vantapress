<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /

    # CRITICAL: Prioritize index.html during fresh install, then index.php after installation
    DirectoryIndex index.html index.php

    # Handle Authorization Header
    RewriteCond %{HTTP:Authorization} .
    RewriteRule .* - [E=HTTP_AUTHORIZATION:%{HTTP:Authorization}]

    # CRITICAL: Allow direct access to install.php (bypass Laravel routing)
    RewriteRule ^install\.php$ - [L]

    # CRITICAL: Serve static assets directly (no public/ folder)
    # Assets are in root: /css/*, /js/*, /images/*, etc.
    RewriteCond %{REQUEST_FILENAME} -f
    RewriteRule ^(css|js|images|fonts|vendor|favicon\.ico)(.*)$ - [L]

    # Redirect Trailing Slashes
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteCond %{REQUEST_URI} (.+)/$
    RewriteRule ^ %1 [L,R=301]

    # Explicitly route admin paths
    RewriteRule ^tcc-admin(.*)$ index.php [L,QSA]
    RewriteRule ^admin(.*)$ index.php [L,QSA]

    # Route everything else to Laravel
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteRule ^ index.php [L,QSA]
</IfModule>

# Disable directory browsing
Options -Indexes

# Protect sensitive files
<FilesMatch "^\.">
    Order allow,deny
    Deny from all
</FilesMatch>

# Block access to sensitive file extensions
<FilesMatch "\.(env|log|md|json|lock|yml|yaml|xml|sql|gitignore|gitattributes)$">
    Order allow,deny
    Deny from all
</FilesMatch>

# Block access to sensitive directories
RedirectMatch 403 ^/(app|bootstrap|config|database|resources|routes|storage|vendor|tests)/

# Block access to composer files
<FilesMatch "^(composer\.(json|lock)|package(-lock)?\.json|artisan)$">
    Order allow,deny
    Deny from all
</FilesMatch>
